<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.codewithganpath.quiz_app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Quiz_App">
        <activity android:name=".JavaLevelsActivity"></activity>
        <activity android:name=".KoltinLevelsActivity"></activity>
        <activity android:name=".PythonLevelsActivity"></activity>
        <activity android:name=".CategoryActivity" />
        <activity android:name=".QuizActivity" />
        <activity android:name=".Result" />
        <activity android:name=".MainActivity" />
        <activity android:name=".DeveloperActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>